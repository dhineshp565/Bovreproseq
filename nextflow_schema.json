{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/Bovreproseq/master/nextflow_schema.json",
    "title": "Bovreproseq pipeline parameters",
    "description": "",
    "type": "object",
    "definitions": {
        "required_parameters": {
            "title": "Required parameters",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Must be provided",
            "required": [
                "input",
                "outdir",
                "reference",
                "primerbed",
                "kraken_db"
            ],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "directory-path",
                    "exists": true,
                    "mimetype": "text/csv",
                    "description": "Path to comma-separated file containing information about the samples in the experiment.",
                    "help_text": "You will need to create a design file with information about the samples in your experiment before running the pipeline. Use this parameter to specify its location. It has to be a comma-separated file with 3 columns, and a header row.",
                    "fa_icon": "fas fa-file-csv"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
                    "fa_icon": "fas fa-folder-open"
                },
                "reference": {
                    "type": "string",
                    "default": "reference.fasta",
                    "format": "file-path",
                    "help_text": "reference file in fasta format (fasta/multi-fasta)"
                },
                "primerbed": {
                    "type": "string",
                    "default": "primer.bed",
                    "format": "file-path",
                    "help_text": "primer bed file with primer co-ordinates"
                },
                "kraken_db": {
                    "type": "string",
                    "format": "path",
                    "help_text": "path to kraken database"
                }
            }
        },
        "optional_parameters": {
            "title": "optional parameters",
            "type": "object",
            "description": "",
            "default": "",
            "properties": {
                "trim_barcodes": {
                    "type": "string"
                },
                "centri_db": {
                    "type": "string",
                    "format": "directory-path"
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/definitions/required_parameters"
        },
        {
            "$ref": "#/definitions/optional_parameters"
        }
    ]
}